import { SkeloadDirective } from './skeload.directive';

describe('SkeloadDirective', () => {
  it('should create an instance', () => {
    const directive = new SkeloadDirective();
    expect(directive).toBeTruthy();
  });
});
