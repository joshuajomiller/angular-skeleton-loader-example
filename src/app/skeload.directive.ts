import {
  AfterContentInit,
  AfterViewInit, ComponentFactoryResolver, ContentChild,
  ContentChildren,
  Directive,
  Input,
  OnChanges,
  OnInit,
  QueryList, ViewChild, ViewContainerRef
} from '@angular/core';

@Directive({
  selector: '[appSkeload]'
})
export class SkeloadDirective  {

}
